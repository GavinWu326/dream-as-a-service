name: dream-as-a-service

services:
  backend:
    build:
      context: ./backend
    environment:
      DREAM_DATABASE_URL: sqlite:////app/data/dreams.db
      DREAM_CORS_ORIGINS: http://localhost:3000
    volumes:
      - dream_data:/app/data
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: /api
    depends_on:
      - backend
    ports:
      - "3001:80"

volumes:
  dream_data:
